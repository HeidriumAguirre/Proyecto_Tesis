chroma_db/
.idea/
data/mineduc_pdfs/
__pycache__/
.venv/

# Secretos y configuración local
.env
*.crt
*.key
docker/certs/

# Certs autofirmados locales
.streamlit/secrets.toml

# Backups de documentos de tesis (no versionados, solo conservados localmente)
*.backup_*.docx
*Tesis_*.backup_*.docx

# Archivos temporales de Office (locks y backups automaticos de Word)
~$*.docx
~$*.doc
~$*.xlsx
~$*.pptx

# Backups automaticos del VPS (no versionar; son binarios grandes)
scripts/backups/db/
scripts/backups/chroma/

# Caddy data y config del VPS (certificados TLS locales)
docker/certs/

